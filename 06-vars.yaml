- name: variables
  hosts: local
  connection: local
  vars: # play level variables
    COURSE: "Devops with aws"
    DURATION: "120hrs"
    TARINER: "sai"
  tasks:
  - name: print the variables
    ansible.builtin.debug:
      msg: "Course is {{ COURSE }}, Duration is {{ DURATION }}, Trainer is {{ SAI }}"